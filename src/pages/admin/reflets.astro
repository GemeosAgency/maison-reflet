---
/** L'ancienne page « Reflets & accords » vit désormais dans Luma (ce qu'elle recommande) et dans Trafic & sources (écoutes, accords). */
export const prerender = false;
return Astro.redirect(`/admin/luma${Astro.url.search}`, 301);
---
