---
/**
 * Section « You may also like » : bande de cartes produit sous un titre
 * centré. Extraite de la fiche produit pour être partagée avec la page
 * coffret — rendu identique.
 */
import ProductTile from "./ProductTile.astro";
import type { ResponsiveImage } from "../lib/sanity";

export interface RelatedItem {
  name: string;
  size?: string | null;
  subtitle: string | null;
  price: string;
  img: ResponsiveImage | null;
  /** Photo du survol (voir ProductTile). */
  hoverImg?: ResponsiveImage | null;
  href: string;
  badge: string | null;
  /** Variante à ajouter au panier depuis le survol. */
  variantId?: string | null;
}

interface Props {
  title: string;
  items: RelatedItem[];
  /** Libellés du bouton d'ajout révélé au survol. */
  addLabel?: string;
  addedLabel?: string;
  errorLabel?: string;
}

const { title, items, addLabel, addedLabel, errorLabel } = Astro.props;
---

{
  items.length > 0 && (
    <section class="related">
      <h2>{title}</h2>
      <div class="related-grid full-bleed">
        {items.map((p) => (
          <ProductTile
            name={p.name}
            size={p.size}
            subtitle={p.subtitle}
            price={p.price}
            img={p.img}
            hoverImg={p.hoverImg}
            href={p.href}
            badge={p.badge}
            variantId={p.variantId}
            addLabel={addLabel}
            addedLabel={addedLabel}
            errorLabel={errorLabel}
          />
        ))}
      </div>
    </section>
  )
}

<style>
  .related { display: flex; flex-direction: column; align-items: center; }
  .related h2 {
    margin: 0;
    padding: 56px var(--gutter-x) 40px;
    font-size: 32px;
    font-weight: 500;
    text-transform: uppercase;
    text-align: center;
  }
  .related-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 2px;
  }

  @media (max-width: 900px) {
    .related h2 { font-size: 26px; padding: 40px var(--gutter-x) 24px; }
    /* Une rangée à swiper remplace les trois rangées de recommandations. La
       carte suivante dépasse suffisamment pour rendre le geste évident. */
    .related-grid {
      display: flex;
      gap: 12px;
      box-sizing: border-box;
      padding-inline: 0;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      scroll-padding-inline: 0;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
    }
    .related-grid::-webkit-scrollbar { display: none; }
    .related-grid > :global(.ptile) {
      flex: 0 0 calc(100vw - 52px);
      scroll-snap-align: start;
    }
  }
</style>
