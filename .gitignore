# build output
dist/
# generated types
.astro/
# sanity studio runtime cache & build
.sanity/
studio/dist/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
# `.env*` attrape aussi .env.local et .env.production.local, les noms
# que `vercel env pull` écrit par défaut — ils contiennent les secrets
# de prod et n'étaient pas ignorés.
.env*
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
.vercel

# Supabase CLI (état local du link, jamais à committer)
supabase/.temp/
